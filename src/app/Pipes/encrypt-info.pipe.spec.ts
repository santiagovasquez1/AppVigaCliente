import { EncryptInfoPipe } from './encrypt-info.pipe';

describe('EncryptInfoPipe', () => {
  it('create an instance', () => {
    const pipe = new EncryptInfoPipe();
    expect(pipe).toBeTruthy();
  });
});
