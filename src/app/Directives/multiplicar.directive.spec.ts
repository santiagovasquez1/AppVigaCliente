import { MultiplicarDirective } from './multiplicar.directive';

describe('MultiplicarDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiplicarDirective();
    expect(directive).toBeTruthy();
  });
});
